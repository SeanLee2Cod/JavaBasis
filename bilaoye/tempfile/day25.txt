
文件)：
 1.配置好环境变量：将Mysql解压缩的目录下的bin的路径加在用户变量的path后即可。


2.添加配置文件：
   把文件夹根目录下的mydefault.ini拷贝一份，命名为my.ini(任放在该目录下)，把
my.ini里面的basedir和datadir的注释去掉，并设置这两个属性。
   #设置mysql的安装目录('#'为注释符)
   #设置mysql数据库的数据的存放目录，必须是data，或者是//xxx/data
    

